language: python
cache: pip
python: 3.6
env: TOXENV=py

matrix:
  include:
    - python: 3.6
      env: TOXENV=runtests,codecov,mypy
    - python: 3.7
      dist: xenial
      env: TOXENV=runtests,codecov,mypy

install:
  - pip install tox-travis

script:
  - cd src/ && tox
