language: python
cache: pip
python: 3.6
env: TOXENV=py

matrix:
  include:
    - python: 3.6
      env: TOXENV=tests,codecov
    - python: 3.7
      dist: xenial
      env: TOXENV=tests,codecov

install:
  - pip install tox-travis

script:
  - cd src/ && tox
